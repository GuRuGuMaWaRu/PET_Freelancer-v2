# Avoid "allowedHosts[0] should be a non-empty string" when lanUrlForConfig
# is empty (e.g. offline, VPN, or hostname resolution issues).
# Only used in development; see CRA advanced config.
DANGEROUSLY_DISABLE_HOST_CHECK=true
